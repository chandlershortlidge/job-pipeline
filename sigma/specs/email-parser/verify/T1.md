# Verify — T1: Scaffold package, deps, gitignore, config

**Verdict: PASS** (independent checker; all commands run this session, 2026-07-22)

## Acceptance criteria

| # | Criterion | Result | Evidence |
|---|-----------|--------|----------|
| 1 | `uv sync` green | PASS | `Resolved 91 packages … Audited 89 packages` — no error, no unresolved. |
| 2 | Two new deps resolve at pinned floors | PASS | `pyproject.toml`: `google-api-python-client>=2.149.0`, `google-auth-oauthlib>=1.2.1`. `uv.lock` locks `2.198.0` (≥2.149.0) and `1.4.0` (≥1.2.1). |
| 3 | `import email_parser.config` works | PASS | `uv run python -c "import email_parser.config"` imports clean; all five constants read back (below). |
| 4 | Token/creds globs gitignored (`git check-ignore` passes) | PASS | `.gitignore:228-230` has `email_parser/.gmail_token.json`, `email_parser/.gmail_credentials.json`, `*.gmail_token.json`. `git check-ignore` returns 0 for `.gmail_token.json`, `.gmail_credentials.json`, and `foo.gmail_token.json` (glob). |
| 5 | Module has top docstring (what it does / does NOT do) | PASS | `email_parser/__init__.py` and `config.py` both carry a top docstring with an explicit "Does NOT:" clause. `__doc__` truthy on both. |

## Constants (spec §3 config block, lines 328-335) vs implementation

| Constant | Spec | `config.py` | Match |
|----------|------|-------------|-------|
| `MODEL_CLASSIFIER` | `claude-haiku-4-5` | `'claude-haiku-4-5'` | ✓ |
| `MODEL_EXTRACTOR` | `claude-sonnet-4-6` | `'claude-sonnet-4-6'` | ✓ |
| `GMAIL_QUERY` | `label:job-search` | `'label:job-search'` | ✓ |
| `GMAIL_CREDENTIALS_FILE` | required | `<pkg>/.gmail_credentials.json` | ✓ |
| `GMAIL_TOKEN_FILE` | required | `<pkg>/.gmail_token.json` | ✓ |

Model ids match the spec verbatim (spec is source of truth; not judged against
external model-id catalogs). Credential paths are package-relative and point only
at gitignored files — config names paths, holds no secrets, matches its docstring.

## Scenario coverage

T1 is scaffolding — the spec attaches **no BDD scenario** to it (scenarios begin at
the matcher/source/classify/extract/pipeline tasks). Nothing to cover; N/A.

## Evidence log (commands run this session)

- `uv sync` → `Resolved 91 packages … Audited 89 packages`
- `grep` pyproject/uv.lock → deps + locked versions above
- `git check-ignore email_parser/.gmail_token.json` → prints path, exit 0 (×3 incl. glob)
- `uv run python -c "…"` → `CLASSIFIER 'claude-haiku-4-5'  EXTRACTOR 'claude-sonnet-4-6'  QUERY 'label:job-search'  … PKG_DOC True  CFG_DOC True`

## Next

→ PASS. Proceed to T2 (`normalize.py`) / T3 (`models.py`) — parallel after T1.
