{
  "name": "scratch",
  "version": "1.0.0",
  "description": "Purpose (from the sprint plan): on a handful of your real screenshots, run the extraction approach and find where it breaks **before** the 5-hour sprint, so the day itself is execution, not discovery.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@daytona/sdk": "^0.190.1"
  }
}
